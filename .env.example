# The default `just demo` does not need credentials.
# Copy this file to `.env.openrouter` only when using `just demo-openrouter`.
OPENROUTER_API_KEY=

# Optional live-demo model override.
OPENROUTER_MODEL=openai/gpt-4o-mini
